import ExecutionStartClient from './ExecutionStartClient';

/** Canonical public-first execution bridge (client-owner). See ExecutionStartClient. */
export default function ExecutionStartPage() {
  return <ExecutionStartClient />;
}
